<%- include('header.ejs') %>
<div class=" container flex justify-center">
    <!-- component -->
    <div class="max-w-md py-4 px-10 bg-white shadow-lg rounded-lg my-20">
        <div class="flex justify-center md:justify-end -mt-16">
        <img class="w-20 h-20 object-cover rounded-full border-2 border-indigo-500" src="<%= body.user.image %>">
        </div>
        <div>
        <div class="text-gray-800 text-3xl font-semibold"><%= body.blog.title %></div>
        <p class="mt-2 text-gray-600"><%- body.blog.body %></p>
        </div>
        <div class="flex justify-end mt-4">
        <a class="text-xl font-medium text-indigo-500"><%= body.user.displayName %></a>
        </div>
    </div>
</div>
<%- include('footer.ejs') %>